export type PromptMessage = {
    location: string,
    chaseLength: number,
}

export type ItineraryItem = {
    challenge: string,
    description: string,
    hint: string,
    name: string, 
    stop_number: string,
}